export const getAuthUrl = (path: string) => {
  return `/auth/${path}`
}
export const getTodoUrl = (path: string) => {
  return `/todos/${path}`
}

export const getProfileUrl = (path: string) => {
  return `/profile/${path}`
}
